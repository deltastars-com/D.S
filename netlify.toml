[build]
  command = "npm install --arch=x64 --platform=linux sharp && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  SHARP_DIST_BASE_URL = "https://netlify-libvips.s3.amazonaws.com"
